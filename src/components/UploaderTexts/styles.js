import {styled} from 'styletron-react'

export const Label = styled('label', {
  display: 'flex',
  alignItems: 'center',
  border: '1px gray solid',
  padding: '20px',
  borderRadius: '5px'
})

export const Input = styled('input', {
  display: 'none'
})

export const Claim = styled('p', {
  marginLeft: '5px'
})

export const UploadClaim = styled('p', {
  border: '1px gray solid',
  padding: '20px',
  borderRadius: '5px'
})
